# Copy to cloud/hosts.env and fill in. hosts.env is gitignored (addresses are ephemeral).
BROKER_PRIV=10.0.1.221        # private IP of the broker host (sbl-b1)
BROKER2_PRIV=10.0.1.242       # cluster node 2 (sbl-b2), only needed for cluster.sh
BROKER3_PRIV=10.0.1.140       # cluster node 3 (sbl-b3), only needed for cluster.sh
SSH_KEY=$HOME/.ssh/oci_sbl
