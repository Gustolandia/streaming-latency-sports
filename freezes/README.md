# Freezes

A freeze is an experiment plan committed here before the experiments it describes are run. It
fixes, in advance, what we predict, how the runs are designed, how many there are, and the rule
that decides whether each prediction held. Researchers call this *pre-registration*.

## Why the plans are committed here

A prediction convinces a reader only if it was written down before the data. Every commit in
this public repository carries a date and an identifier, and neither can be changed later
without the change showing (a commit is a saved version of every file in the repository). So a
plan committed here shows what was predicted, and how it would be judged, before the first run
it covers.

## The rules

1. **A freeze is never edited.** A change is a new freeze in a new folder, numbered in order.
   Its first section lists what changed since the previous freeze and why, and says whether a
   result already seen prompted the change.
2. **Every file in a freeze has a fingerprint** in the folder's `SHA256SUMS` (a fingerprint is a
   64-character code computed from a file's bytes; changing one character changes the code), so
   a copy found anywhere can be checked against this one. Git stores these files byte for byte,
   so the fingerprints hold on every computer.
3. **Runs start after the commit is public.** No run a freeze covers starts before the freeze is
   on GitHub.
4. **Results are reported against the freeze in force when they ran**, and that includes every
   prediction that failed.

## The freezes

| Folder | Frozen | What it fixes |
|---|---|---|
| [`01-experiment-plan/`](01-experiment-plan/) | 16 September 2026 | the law campaign on Azure: its predictions, campaigns, repeats rule and analysis, and how its runs are checked, stopped and kept |
| [`02-experiment-plan/`](02-experiment-plan/) | 17 September 2026 | amends 01 after its first runs, which were stopped, and starts stage 0 again: the calibration known within 0.3 ms, the delay checked by the broker's own capture, the paper's Redis setting, M0 on bursty traffic, and runs protected from faults of the machines; prompted by results, as its first section says |
| [`03-experiment-plan/`](03-experiment-plan/) | 18 September 2026 | amends 02 after its first shakedown: every judgement about the added delay is read from the broker's own capture, where the delay is applied, and the paths are recorded by ping, TCP and UDP rather than judged, because ping is not the path the messages take |
| [`04-experiment-plan/`](04-experiment-plan/) | 18 September 2026 | amends 03 after stage 0 ran on all three pairs: a slice whose plateau lies below the client's own zero-delay trip is reported as out of reach and not run, and P1 and P9 are judged on the slices each pair can reach; the brake that stops a session on a got-it shift must clear that session's own zero-delay scatter, after a sound session was stopped by a brake set at 0.056 ms |
| [`05-experiment-plan/`](05-experiment-plan/) | 19 September 2026 | amends 04 after the rounds rule was run for A3 and no measured run was read: P3 asked two things at once and was confirmed only where both held, so its power was its weaker half's, and that half answered the same whether the law was true or false. It becomes P3a (load leaves the cliff where it is) and P3b (load raises the plateau), each with its own falsifier. P3a is judged by the whole interval of the movement rather than by the point estimate, which is how a claim that something did *not* change has to be made; A3 runs 16 rounds |
| [`06-experiment-plan/`](06-experiment-plan/) | 20 September 2026 | changes no rule and corrects how one was applied. Rounds are set at the spread a pair's pilot measured *for the backend the campaign runs*, as the plan has said since version 4; one figure pooled over both backends was used instead. Pooling assumes the two share a variance, and the pilots refute it — 0.213 for Kafka against 0.701 for Redis on the second x86 pair. What the departure cost is checked campaign by campaign and cost nothing: A1, A4 and A7 all clear the bar at their own backends' spreads |
| [`07-experiment-plan/`](07-experiment-plan/) | 20 September 2026 | settles what 06 left open: an A5 session measures its own baseline at its own core count, not only its own calibration. The baseline is the floor every trip is placed from and it moves with the core count for the same reasons the calibration does, so a two-CPU session would otherwise place its trips from a floor measured at eight |
| [`28-experiment-plan/`](28-experiment-plan/) | 26 September 2026 | finishes what was started: every campaign from here on runs with the got-it brake recording rather than stopping, as A5's last runs did (D26-1), and every campaign that stopped part-way is finished from its own queue, under its own calibration, on the kernel it ran on (resume.sh). Corrects the two faults of the kit that cut runs, M0's receiver capture that was never stopped and the queue's twelve-hour guard, and registers every run's machine and settings beside every fault met. No prediction changes |
| [`27-experiment-plan/`](27-experiment-plan/) | 25 September 2026 | fixes how M0 is read, before any of its runs is read (m0_read.py, pcap_read.py): the departure signed and read on the recorded runs, an explanation accounting for half of it only by pushing the same way; M-H2's replay a simulation of the receive loop from its own rules and costs, never from its outcomes; and, since both clients switch Nagle's algorithm off, M-H4 run as written and not for Kafka. No prediction, test or run changes |
| [`26-experiment-plan/`](26-experiment-plan/) | 25 September 2026 | makes the five corrections version 27 recorded in the programs that judge: P3a and P3b compare the loads whose plateau is at least 2%, P7 is held to P1's band alone, a campaign that cannot reach what a rule reads is reported out of reach, a block of campaigns is resampled campaign by campaign, and P6 has a program (helper_waits.py p6). Fixes how A9 is read where the plan left it open, and adds A9-2b, the rate predicted message by message, beside the A9-2 version 28 froze. M0's reading comes in version 31. No prediction and no run changes |
| [`25-experiment-plan/`](25-experiment-plan/) | 25 September 2026 | builds M0's recording, which was never built, and runs M0 as stage 0 always planned: the football match replayed in bursts on the first x86 pair, at 0, 2 and 8 ms and 10 rounds, recording in half its runs the packets at both ends, the receive loop's cycle and every python3 thread's scheduling events. It changes no prediction and no test of M-H1 to M-H4. A run replaying the match in bursts is held to that plan's own count and rate in its window, and a Redis setup to its own acknowledgement batch -- held to the steady rules, every such run would have been repeated for doing what it was asked. The corrections version 27 records come in version 30 |
| [`24-experiment-plan/`](24-experiment-plan/) | 25 September 2026 | adds A9, a study of the mechanism on all three pairs, and changes no prediction. The law is about the waits of the one thread that stamps each acknowledgement -- kafka-python's own network thread, or the Redis producer's send workers -- and A6's recording counts every python3 thread's waits in one histogram. A9 records every python3 thread's scheduling events one by one in half its runs, the clients name the threads that stamp, and each negative reading is read against the waits of the thread that stamped its acknowledgement. How it is read, and P6's prediction from that one thread, are fixed before its runs. M0, with the recording it never had, and the corrections version 27 records, come in version 29 |
| [`23-experiment-plan/`](23-experiment-plan/) | 25 September 2026 | adds the runs that could still improve what the testbed measured before it is taken down, and records what a check of all the data found in the programs that judge; it changes no prediction. A2's Redis session on our HZ=1000 build had one day of its two, and runs its second; each backend gets a second bridge day on the stock kernel, because the bridge missed its 0.1 ms by up to 0.5 ms while our build's two days agreed within 0.07; A1 gains a 3.75 ms slice, because at the A1 sessions' own zero-delay trip Kafka reached three slices where P1 asks for four; and a third x86 A3 campaign runs at 75 and 88% load, the loads P3a and P3b are judged at under D14-2 -- which the judge itself never applied, comparing 50% with 88% in every A3 verdict. That and four more places where the judging programs depart from the text are recorded here and corrected in version 28, with every verdict they touch judged again |
| [`22-experiment-plan/`](22-experiment-plan/) | 25 September 2026 | records the corrections found on 25 September and changes no prediction. A5's session at two CPUs stopped twice on its got-it brake -- a move of 0.505 ms with 0.98 ms added, then 0.660 ms with 0.046 ms added, where the delay cannot be what moved it -- and its last thirteen runs are run with the brake recording rather than stopping. That change was decided after both stops, is frozen here before the runs it covers, and P7 at two CPUs is judged both with and without them (D26-1). T2 holds a tool's count against what the tool was asked to send, not against the trips of our reference, which is another run of another client; one verdict becomes undecided and no tool's answer changes (D26-2). The Go tools' T2 moves the driver's own clock and puts it back after every run, checked (D26-3). The tools block runs the four rounds D4-2 always asked for, each in a folder of its own (D26-4). And the plan's version history, one box that could not break across a page, had been cut from the PDF below page 3; it is now one box per version (D26-5) |
| [`21-experiment-plan/`](21-experiment-plan/) | 24 September 2026 | records every correction found on 24 September and changes no prediction. A check of the running campaigns was turned on the instrument itself, and three of the eleven tools turned out to have measured nothing while reporting figures or errors that looked like readings: k6 made no request at all -- `sudo` cleared the environment its URL and rate came through -- and reported the time its failures took, 9 microseconds, which the reader took for a latency; nats-latency and rabbitmq-perftest met servers that could not answer them, because the setup meant for the broker had been run on the driver and certified there by asking 127.0.0.1. Those runs are set aside and run again, and the servers are now set up on the broker and checked from the driver before every stage. T2's judge compared against the wrong run and had no allowance for a tool's own run-to-run noise, so a tool whose figures did not move was ruled out of "did not move" by its own scatter; it is given the noise from the ten runs of each tool's T1 staircase, judges the one-clock hypothesis beside the four behaviours, and predicts at the offset measured at the clock -- fixed here before any reading is judged under it. T1's slope is recorded for every tool and held to the number of times its interval crosses the delayed direction, which is how k6 was found. The Arm pair's A3 is read at the loads it ran at (52.7% for the level designed as 50). Green tags now mark only this version's changes; fifty-four from fifteen earlier versions had stayed green |
| [`20-experiment-plan/`](20-experiment-plan/) | 24 September 2026 | gives the Arm pair one more campaign and changes no prediction. A3 asks what load does -- whether it leaves the cliff where it is and whether it raises the plateau -- and both questions have only ever been judged on the first x86 pair. A4 already asks A1's question of a second processor family; nothing asks this one. So A3's Kafka campaign runs on the Arm pair as well, at 24 rounds, the number P3a's own simulation gives at that pair's measured spread and levels: confirmed in 88% of campaigns under the law and in 0% where it is false. Its Redis half is not run there, because that pair's own spread pilot reads a plateau of 0.0115 and a floor its reader marks as below resolution -- the same shallow curve that puts P3a and P4 out of reach on Redis on the x86 pair. And P3b is recorded as underpowered before a single run of it exists: 76% at the ceiling of 40 rounds where the rule asks 80%, so a non-confirmation of it on the Arm pair is a limit of this instrument rather than evidence against the prediction. Nothing is pooled across pairs: the x86 answers stand as they are |
| [`19-experiment-plan/`](19-experiment-plan/) | 22 September 2026 | adds an addendum at the end and one run to each T2 session, and changes no prediction. The tools block ran for the first time that day, and running it showed something about T2 that reading it had not: T2 moves the clock a tool reads and asks what it does with the values that come out below zero, and that question has an answer only where the tool's subtraction spans two clocks. Ten of the eleven tools are invoked as a single process that writes both timestamps itself, so the moved clock moves both and no value below zero can arise; the eleventh, rdkafka_performance, is the only one with the producer on the real clock and the consumer on the moved one -- and its consumer read none of the producer's messages, so it measured nothing at all. Neither is a fault in the tools or in T2. It is the result: a benchmark that reports a round trip against its own clock cannot exhibit this artifact, and cannot measure the one-way latency a streaming system cares about either. So a T2 session now runs three times rather than two -- once at no offset as the control, once at each offset, with the shift actually achieved measured at the clock -- and where the clock moved and the tool's figures did not, the verdict is that the tool times against one clock. rdkafka_performance's arrangement is established by a shakedown before anything of it is measured, and where no invocation reads the producer's messages that is T4's answer for it, reported as this harness failing to drive the tool rather than as a limit of the tool |
| [`18-experiment-plan/`](18-experiment-plan/) | 22 September 2026 | reads one gate across the staircase instead of step by step and changes no prediction. The gate asks whether the added delay is reaching the "got it" note, which would spoil the note every later run is held against. Leakage of a fraction of the delay is that fraction at every step, so it is a slope; the test was a quarter of the delay step by step, which is not the same thing. A note that drifts by a fixed amount fails wherever that amount is more than a quarter of the delay added -- every step below four times the drift, and no step above it -- so the rule ends sessions at their smallest step and is blind to the same wander at their largest. The second x86 pair showed both halves of that in one calibration: Redis shifted +0.342 ms at 1 ms added and the session was ended after an hour of calibrating, while Kafka shifted -0.315 at 4 ms and -0.282 at 8, both further from zero than their own 90% intervals reach and both larger than the one that failed, and passed. Their slopes against the delay are -0.010 and -0.038: nothing was leaking in either. The test is now that slope, above a quarter, with a shift larger than the whole delay added still gross at any one step on its own, and the slope written out beside the gate. Everything else about the note is unchanged |
| [`17-experiment-plan/`](17-experiment-plan/) | 22 September 2026 | moves half of one campaign to the other x86 pair and changes no prediction. A2's six Kafka sessions and its Kafka bridge run on the first x86 pair; the second keeps the six Redis sessions, the Redis bridge and A5's two remaining sessions. Every prediction here is judged within one pair and one backend and nothing is pooled across either, so what has to stay together is a backend's three kernels rather than the two backends, and each keeps its counterbalancing of each kernel on two days. The load is what changed: the first pair finishes the ten tool campaigns today and the second was carrying about forty-five hours alone, so this has the last measurement land about nineteen hours earlier, and the HZ=1000 Kafka session the second pair has already run becomes a replicate of that cell on a second machine. Also: a prediction is now judged on the runs the integrity rule passed, and the answer names the ones it left out -- the reader kept anything with a trip and a negative rate, whatever the brake had said, although the brake has always refused to compare a run against neighbours that did not count. Five runs of 3,179 are affected: A3's two answers reproduce to four decimals and A8's ratio moves from 3.12 to 3.25, not confirmed either way. And A4's P9 was read with the block's four campaigns in one folder, so every run carried that folder's name and the anchor correction moved everything by zero while the answer said otherwise; read as the plan asks, the free slope is 0.830 on Kafka and 1.153 on Redis, both inside the band, and what fails is the fitted summary of the same curves |
| [`16-experiment-plan/`](16-experiment-plan/) | 22 September 2026 | widens two brakes and changes no prediction. Neither touches a measurement: every run is recorded as before, and P5(c) is judged from the calibration on the same numbers. The calibration's precision bound now follows the machine's tick -- the larger of 0.30 ms and 0.15 of it -- because 0.30 was set on a 1 ms tick and A2 boots kernels where it is 4 and 10, and a calibration cannot be known to a fraction of the quantum its trips are quantised by. Kafka on the second x86 pair came in at 0.410 at HZ=250 and 0.387 at HZ=100 against Redis's 0.128 to 0.237 on the same boots, and four rounds did not move it. The got-it brake's floor goes from 0.10 ms to 0.25: A8's x86 campaign stopped at 86 of 144 on a move of 0.134 ms where its own pooled scatter allowed 0.132, an eight-hour campaign ended on two microseconds, and at 0.10 the floor sat below the drift sound campaigns show. And a campaign is gated on the calibration it is placed from rather than every one in the file, which had cost three sessions and about seven and a half hours |
| [`15-experiment-plan/`](15-experiment-plan/) | 22 September 2026 | corrects a program and no prediction. P8 has three clauses -- Java shows the same cliff, go-first cuts its plateau at least five times, and Python's rate is at most 1.5 times Java's -- and the judge tested two. It printed the rule it applied, naming the cliff and the ratio, while its own documentation said it tested the priority effect; a judge that omits a clause its campaign passes returns exactly what a complete one would, so no verdict could show it. Found by reading the rule printed against the rule stated, while judging A8's first finished campaign. The cut is now computed at the trips both clients ran, gated at five, and recorded beside the verdict, and that campaign judged again under all three clauses returns the same answer -- not confirmed, on the ratio at 4.33 -- with a cut of 469.7, the figure computed by hand from the same runs. Also: the ten tool campaigns run on one x86 pair rather than five on each, which changes no rule, since what the plan requires is that each tool runs entirely on one pair |
| [`14-experiment-plan/`](14-experiment-plan/) | 21 September 2026 | widens the same brake, and does so after it stopped a campaign — the order of events a reader should distrust, so the argument is stated without it. Version 17 allows three times the scatter of the judged setup's own earlier runs and lets the brake judge as soon as two exist, so the first braked run of every setup is measured against a standard deviation from two points, which has one degree of freedom and is not an estimate of spread. A2's first session gave ten within-setup scatters from 0.053 to 0.227 ms around a pooled 0.133, and the 0.053 stopped it — a run 0.275 ms from its centre where three times the pooled figure is 0.398. The scatter belongs to the sitting, so it is pooled across the campaign's setups; the centre is unchanged, so no drift from another sitting enters; and the brake waits for five degrees of freedom. The pooled figure and its degrees of freedom are recorded beside every run. Not switched off: a run a millisecond from its centre still stops a campaign, and nothing is cut — a run the brake cannot yet judge still counts |
| [`13-experiment-plan/`](13-experiment-plan/) | 21 September 2026 | corrects the same brake version 12 corrected that morning, and in the other direction: 12 settled which runs the brake may judge, this settles what it judges them against. It held every run's “got it” median against the session's calibration — a different sitting, hours earlier — and allowed it a quarter of the delay that run added. Those are two different quantities. What separates a campaign from its calibration is drift, which does not grow with the delay, while the allowance does, so the allowance is smallest exactly where the drift has nowhere to go. A3's Kafka campaign drifted 0.022 ms from its calibration over 216 judged comparisons and A8's drifted 0.124 over 14, on noise floors of 0.170 and 0.199 — near-equal floors and a six-fold difference in drift — and the brake bit at exactly one rung, the shortest delay of the client needing the least delay, where its allowance falls to 0.221 ms. A run is now held against its own campaign's earlier runs of the same setup, which share its client, its note's place, its slice and its delay, so no drift enters the comparison. The distance from the calibration is still computed and recorded and stops nothing; P5(c) stays where the plan has always put it, in the session's own calibration, which varies the delay with everything else fixed. Not loosened: checked against A8's own runs, a campaign that broke mid-run would still stop |
| [`12-experiment-plan/`](12-experiment-plan/) | 21 September 2026 | corrects one brake and no prediction. P5(c) asks whether the added delay moved the "got it" time, and the brake enforcing it compared every run against the session's calibration. A8 takes that note two ways, and taking it inline forces the producer to one message in flight where the calibration ran with sixty-four — two different producers, on purpose, because that is the treatment A8 exists to measure. So the comparison asked what the treatment did, not what the delay did. On the Arm pair it stopped A8 on its first Python run, 0.167 ms against a 0.145 ms brake, while the Java runs — whose two medians happen to lie 0.06 ms apart — passed the same brake untouched. A run whose note is taken a way the calibration never took it is now recorded rather than braked, with its median and the note's place beside it, and P5(c) is asked of those runs by comparing the ones that share a note's place across the delays they ran at. A campaign also counts, from its own queue before anything runs, how many of its runs the brake can judge; both this fault and the one the evening before were visible there before a machine was started. The brake is not loosened: a run measured the calibration's way, moving that far, still stops |
| [`11-experiment-plan/`](11-experiment-plan/) | 21 September 2026 | changes the tools block and nothing in the law, after T1 and T2 were run against made-up tools before any real one. T1 works as frozen: it reports the chopping class at 0.9 ms where every other class reports 0.1 ms. T2 does not. At the 0.5 ms offset this plan fixed, not one value of 3,000 goes below zero; at 2 ms a tool reading whole milliseconds still meets none, because truncating toward zero — what Java and C do — puts everything between minus one step and plus one step at zero, so its rule for negatives never runs. Five of the ten tools read such a clock. The offsets are now taken from the session's own median trip, what each behaviour would have given is worked out through the step T1 measured, and the verdict names what survives being ruled out rather than what is nearest — the first form of that rule named a tool as replacing negatives with zero when its clock had erased them before its handling ever ran. Each figure a tool prints now carries its own step, because two of them print an exact average beside percentiles chopped to whole milliseconds. Names the ten tools that run and the servers they speak to |
| [`10-experiment-plan/`](10-experiment-plan/) | 20 September 2026 | the first version written after a campaign was run and read. A3's Kafka campaign confirms that load raises the plateau and leaves P3a unresolved — and not for want of rounds: at 50% load the negative rate is 0.15%, and the position of a cliff in a curve that shallow is not determined by any amount of running. Read off that campaign's own runs without a model, the halfway point there does not converge as rounds are added (0.474, 1.111, 1.280, 0.625 ms at 5, 8, 11 and 15 rounds), and giving a point the counts a three-times longer run would have moves it by 0.002 ms. So P3a is judged where the ordinary plateau is at least 2%, the guard P4 has carried since version 4 — 9% power becomes 98%, with false confirmations at 1% — and a load below the bar is reported as out of reach, as an unreachable slice already is. The rounds rule is simulated at the plateau and floor the pilot measures rather than at 0.30 and 0.01, and the made-up world counts negatives out of a run's messages instead of multiplying a rate. Records that the tick predictions' rounds cannot be simulated at all |
| [`09-experiment-plan/`](09-experiment-plan/) | 20 September 2026 | corrects the instrument and no prediction, before A8 runs. The program judging P8 read the middle of the cliff from a trip A8 does not run — the plan has said *s* + 0.5*h* since version 8 — so no result whatever could have satisfied it: it confirmed in 0% of simulated campaigns at every number of rounds from 4 to 40, and the rounds rule reported that as forty rounds and underpowered, which is 1,440 runs and about 84 hours to learn nothing. Corrected, P8 asks 6 rounds. That rule now tells a rule that never confirms from one merely weak; P8's ratio is taken only where both clients ran, and says whether the campaign tested P8 at all; and both clients measure the clock they stamp with and refuse one coarser than 25 µs, which is a tenth of the narrowest bound this plan pre-registers |
| [`08-experiment-plan/`](08-experiment-plan/) | 20 September 2026 | fixes what each tool is predicted to do, before any tool campaign runs, from the documentation and source audit the plan asks for. Seven tools that carried a question mark are classified with the version each was read at, and each prediction's falsifiable content is stated. Records one earlier classification as **refuted** — RabbitMQ PerfTest does not keep negatives, it takes their size, so a clock offset is reported as delay — and records two of the audit's own eight search terms as weak, without changing them |

## Checking a freeze

In the freeze's folder, on Linux, macOS or Git Bash (the program recomputes every fingerprint
and compares it with the list; each file should report `OK`):

```bash
sha256sum -c SHA256SUMS
```

## Pocket dictionary

| Word | Plain meaning |
|---|---|
| Freeze | a plan committed here before its runs, and never edited afterwards |
| Pre-registration | the research practice of fixing predictions and analysis before the data exist |
| Commit | a saved, dated version of every file in the repository |
| Fingerprint (SHA-256) | a 64-character code computed from a file; any change to the file changes it |
| Amendment | a later freeze that changes an earlier one, with its reasons |
| Run | one measurement: a few minutes of messages through a broker |
