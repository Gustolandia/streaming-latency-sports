# Streaming Latency Sports Benchmarks - Development Dependencies
# =============================================================
# Install: pip install -r requirements.txt -r requirements-dev.txt
#
# Kept deliberately small: what you need to run the test suite. The runtime
# libraries the scripts import (pandas/numpy/scipy/matplotlib/seaborn/pyarrow)
# live in requirements.txt.

# --- Testing ---
pytest>=7.0.0                  # test framework (8.x supported)
pytest-cov>=3.0.0              # coverage, incl. --cov-branch (project bar: >=95% branch)
pytest-xdist>=3.0.0            # optional: parallel test execution (-n auto)

# --- Optional tooling (uncomment if you use it locally) ---
# mypy>=0.900                  # static type checking
# flake8>=4.0.0                # linting
# black>=22.0.0                # formatting
# isort>=5.10.0                # import sorting
# pre-commit>=2.20.0           # git hooks
# mkdocs>=1.3.0                # docs site
# ipython>=8.0.0               # interactive shell
